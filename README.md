# Patika SQL Çalışmaları

Bu repository, **Patika.dev SQL eğitimleri** kapsamında çözdüğüm SQL alıştırmalarını ve ödevleri içermektedir.  
Sorgular, her ödev için **ayrı SQL dosyaları (sayfa sayfa)** olacak şekilde eklenmiştir.

Amaç; temel SQL konularını pekiştirmek, sorgu mantığını geliştirmek ve GitHub portföyünde referans olarak sunmaktır.

---
## 📄 Dosya Yapısı

Repository içerisinde her SQL dosyası, **ayrı bir ödevi veya konuyu** temsil etmektedir.

- **Ödev1 – Ödev7**  
  Temel SQL sorguları  
  (`SELECT`, `WHERE`, `ORDER BY`, `LIKE`, `COUNT`)

- **Ödev8**  
  Tablo oluşturma (`CREATE TABLE`) ve örnek veri ekleme

- **Ödev9**  
  `INNER JOIN` kullanımı ve çoklu tablo sorguları

- **Ödev10 – Ödev13**  
  - JOIN türleri (INNER, LEFT, RIGHT)
  - `GROUP BY`, `HAVING`
  - Toplama ve istatistik fonksiyonları (`SUM`, `AVG`, `COUNT`)
  - Alt sorgular (Subquery)
  - Gerçek senaryoya yakın SQL analiz soruları

---
## 🧠 Kullanılan SQL Konuları

- SELECT / WHERE
- ORDER BY / LIMIT
- LIKE / IN / BETWEEN
- Aggregate fonksiyonlar (`COUNT`, `SUM`, `AVG`, `MIN`, `MAX`)
- GROUP BY / HAVING
- JOIN işlemleri
- Subquery (Alt sorgular)

---
## 🎯 Amaç

- SQL temelini sağlamlaştırmak  
- Mülakat ve teknik test sorularına hazırlık  
- GitHub üzerinde düzenli ve anlaşılır SQL örnekleri sunmak  

---

**Hazırlayan:** cerenwert
